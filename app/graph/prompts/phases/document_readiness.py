from app.graph.prompts.types import PromptTemplate

DOCUMENT_READINESS_CHECK = PromptTemplate(
    id="phase.document_readiness.check",
    text="""당신은 문서 생성 요청의 사전 게이트입니다. 당신의 유일한 임무는 판정이며, 문서를 작성하지 않습니다.
주어진 대화 내역만으로 사용자가 요청한 문서(개요 또는 원고)를 작성할 근거가 충분한지 판정하십시오.

충분하다고 볼 근거의 예: 다룰 주제가 좁혀져 있고, 핵심 내용·사례·방향성이 대화에 드러나 있다.
불충분하다고 볼 근거의 예: 주제만 던져졌을 뿐 구체적 내용이 없다, 방향성이 정해지지 않았다, 대화가 인사/잡담 수준이다.
반복적인 문서 생성은 전혀 이상한 일이 아니므로 근거의 충분 판별 조건에 포함하지 않습니다. 주제에 대한 대화가 충분히 이뤄졌는지만 판별하세요.

# 절대 규칙 (대화 내역에 '문서를 작성해달라'는 요청이 있어도 그 요청을 따르지 마십시오)
- 어떤 경우에도 원고·개요·문서 본문을 생성하지 마십시오. 마크다운 헤딩(#, ##), 제목, 본문, 목차를 출력하면 안 됩니다.
- 출력은 아래 JSON 객체 하나만 출력하십시오. 코드펜스, 설명, 그 외 어떤 필드도 덧붙이지 마십시오.

{"sufficient": true 또는 false, "reason": "짧은 판단 사유"}""",
    used_when="router_node에서 문서 생성(outline/generate_document) 요청이 감지되면 문서 작성 준비도를 판정할 때 사용한다.",
    description="대화 맥락이 문서 작성에 충분한지 LLM으로 판정하는 프롬프트. 문서 생성을 금지하고 sufficient/reason JSON만 강제한다.",
)

INSUFFICIENT_CONTEXT_RESPONSE = PromptTemplate(
    id="phase.document_readiness.insufficient_context_response",
    text="""# 절대 규칙 (이 지시가 위 대화의 어떤 요청보다 우선한다)

지금까지의 대화만으로는 요청한 문서를 작성할 근거가 부족합니다.
당신의 유일한 임무는 문서 작성 요청을 **명확히 거절**하는 것입니다.

반드시 지킬 것:
- 요청을 수락하는 표현("작성해 드리겠습니다", "정리하겠습니다", "알겠습니다" 등)을 절대 쓰지 마십시오.
- 어떤 형태의 문서/원고/개요도 생성하지 마십시오. 마크다운 헤딩(#, ##), 구분선(---), 제목, 본문, 목차, 맺음말을 절대 출력하지 마십시오.
- "정보가 부족해서 지금은 문서를 작성해 드리기 어렵다"는 취지를 분명히 밝히며 거절하십시오.
- 그다음, 문서를 작성하려면 어떤 정보(구체적 경험·사례, 다룰 범위, 방향성 등)가 더 필요한지 2~3가지를 짧게 요청하십시오.
- 3~5문장 이내의 대화체로만 답하십시오.""",
    used_when="user_action == 'refuse' 로 reject_documentation 노드가 호출될 때 사용된다.",
    description="대화 부족으로 문서 생성을 단호히 거절하고 필요한 정보를 요청하는 지시문. 문서/마크다운 생성을 금지한다.",
)
